# Generated by chezmoi. Repositories opt in by committing .worktreeinclude.
# Blocking pre-start prevents an agent or dev command from racing its .env.
[pre-start]
copy-ignored = "wt step copy-ignored --require-include"
